# .gitignore 配置说明

本项目的 `.gitignore` 文件已配置为忽略所有不应提交到版本控制的文件。

## 📋 忽略的文件类别

### 🔨 构建输出
- `bin/`, `obj/` - .NET 构建输出目录
- `publish/` - .NET 发布输出目录（本地构建的部署文件）
- `dist/`, `out/` - 其他构建输出
- `Debug/`, `Release/` - 构建配置目录

### 📦 依赖包
- `packages/` - NuGet 包（由包还原自动管理）
- `node_modules/` - Node.js 依赖（如果有前端工具）
- `*.nupkg`, `*.snupkg` - NuGet 包文件

### 🔧 IDE 配置
- `.vs/` - Visual Studio 配置
- `.vscode/` - VS Code 配置（保留扩展推荐和设置）
- `.idea/` - JetBrains Rider/IntelliJ 配置
- `*.suo`, `*.user` - 用户特定配置

### 🐳 容器化
- `Dockerfile.generated` - 自动生成的 Dockerfile
- `docker-compose.override.yml` - 本地覆盖配置

### 📊 数据和日志
- `logs/`, `*.log` - 日志文件
- `dump.rdb`, `appendonly.aof` - Redis 数据文件
- `deployment/prometheus/data/` - Prometheus 数据
- `deployment/grafana/data/` - Grafana 数据
- `deployment/consul/data/` - Consul 数据

### 🔐 敏感信息
- `.env*` - 环境变量文件
- `secrets.json` - 密钥文件
- `appsettings.Production.json` - 生产环境配置
- `*.pfx`, `*.p12`, `*.key`, `*.pem` - 证书和密钥文件

### 🤖 自动生成的代码
- `*_pb.cs`, `*_grpc.cs` - Protobuf 生成的 C# 代码
- `*_pb.js`, `*_grpc_pb.js` - Protobuf 生成的 JavaScript 代码

### 🖥️ 操作系统文件
- `.DS_Store` - macOS 文件
- `Thumbs.db` - Windows 缩略图
- `Desktop.ini` - Windows 文件夹配置

### 🧪 测试结果
- `TestResult*/` - 测试结果目录
- `coverage/` - 代码覆盖率报告
- `*.trx` - 测试结果文件

### 📝 临时文件
- `*.tmp`, `*.temp` - 临时文件
- `*.bak`, `*.old`, `*.orig` - 备份文件
- `*.swp`, `*.swo`, `*~` - 编辑器临时文件

## ✅ 保留的文件

以下重要文件会被跟踪（即使在被忽略的目录中）：

- `.gitkeep` - 保留空目录
- `.gitignore` - Git 忽略配置本身
- `.editorconfig` - 编辑器配置

## 🎯 关键忽略项说明

### `publish/` 目录
这是本地构建部署脚本 (`deploy-services-local.sh`) 创建的发布目录。包含：
- 编译后的 DLL 文件
- 运行时依赖
- 临时的 Dockerfile

**原因**: 这些文件是由本地构建生成的，不应提交到版本控制。每次部署时都会重新生成。

### `deployment/*/data/` 目录
包含运行时的数据文件：
- Consul 的服务注册数据
- Prometheus 的时序数据
- Grafana 的仪表盘配置

**原因**: 这些是运行时数据，不是源代码的一部分。

### 环境变量文件
所有 `.env*` 文件都被忽略，因为它们可能包含敏感信息。

**原因**: 
- 避免意外提交密钥、密码等敏感信息
- 不同环境的配置应该分开管理

## 📝 使用建议

### 检查忽略状态
```bash
# 查看特定文件是否被忽略
git check-ignore -v <文件路径>

# 查看所有未跟踪的文件
git status --ignored
```

### 强制添加被忽略的文件
如果确实需要添加某个被忽略的文件：
```bash
git add -f <文件路径>
```

### 查看当前 Git 状态
```bash
# 简洁模式
git status --short

# 包含被忽略的文件
git status --ignored
```

## 🔍 验证配置

运行以下命令验证重要目录是否被正确忽略：

```bash
# 应该显示 publish/ 被忽略
git check-ignore -v publish/

# 应该显示 bin/ 被忽略
git check-ignore -v src/Gateway/Gateway/bin/

# 应该显示 obj/ 被忽略
git check-ignore -v src/Gateway/Gateway/obj/
```

## 🚨 注意事项

1. **敏感信息**: 永远不要提交包含密码、API 密钥或其他敏感信息的文件
2. **大文件**: 避免提交大型二进制文件或生成的文件
3. **环境特定**: 本地环境配置不应影响其他开发者
4. **定期审查**: 定期检查 `git status` 确保没有意外提交不需要的文件

## 📚 相关命令

```bash
# 清除 Git 缓存（在修改 .gitignore 后）
git rm -r --cached .
git add .
git commit -m "Update .gitignore"

# 查看所有被跟踪的文件
git ls-files

# 查看被忽略的文件
git ls-files --others --ignored --exclude-standard
```

---

**最后更新**: 2025年10月14日  
**维护者**: Go-Nomads 团队
