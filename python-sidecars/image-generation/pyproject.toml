[project]
name = "go-nomads-image-generation-sidecar"
version = "0.1.0"
description = "Internal AI image generation sidecar for Go Nomads backend migration"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
